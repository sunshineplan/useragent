name: Update
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 1'

jobs:
  update:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Check Update
        id: check_update
        run: |
          cd update
          go build -ldflags "-s -w" -o ../run
          cd ..
          ./run -url ${{ secrets.URL }}
          git config user.name "GitHub Actions"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git diff-index --quiet HEAD || echo "new_data=1" >> $GITHUB_OUTPUT

      - name: Date
        if: steps.check_update.outputs.new_data == 1
        id: date
        uses: actions/github-script@v7
        with:
          script: core.setOutput("date", new Date().toLocaleDateString('zh-CN', { timeZone: 'Asia/Shanghai' }))

      - name: Commit
        if: steps.check_update.outputs.new_data == 1
        run: |
          git commit -m ${{ steps.date.outputs.date }}
          git push

      - name: Send backup email
        if: steps.check_update.outputs.new_data == 1
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SERVER }}
          server_port: 587
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          subject: UserAgent Update Success - ${{ steps.date.outputs.date }}
          html_body: file://README.md
          convert_markdown: true
          from: Github Actions
          to: ${{ secrets.TO }}
